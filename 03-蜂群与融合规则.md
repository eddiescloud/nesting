# 蜂群与融合规则

以下数字是动画控制值，不是心理测量。V0.1 统一取 0–1。每段话生成一组参数；两段话一起交给模型，另生成关系参数。

| 参数 | 低值表现 | 高值表现 | 转译依据 |
|---|---|---|---|
| energy | 缓慢滑行 | 快速短促运动 | 当事人描述的激动、紧绷或迟缓；不是伤害等级 |
| dispersion | 集中靠近 | 散开、远离群体中心 | 收缩与疏离的空间隐喻 |
| turbulence | 平稳连续 | 方向反复改变 | 犹疑、冲突或不安的描述 |
| trace | 轨迹快速淡出 | 轨迹在本次结尾仍可见 | 用户是否提到反复想起；不预测真实持续年限 |

## 视觉实现建议

每组固定 48 个蜂形粒子，不随时间出生。速度 = 12 + 60 × energy 像素/秒（以 1080 高画布为基准，按画布高度缩放）。群体半径 = 20 + 100 × dispersion 像素；扰动幅度 = 2 + 22 × turbulence 像素。残留透明度 = 0.12 + 0.48 × trace。数值只是首轮起点，现场按设备调整。

两组使用两个固定、亮度相近的色相；颜色用于识别来源，不编码好坏。使用人形内部整体空间，暂不把一句话映射到特定器官。

## 两组如何产生新结构

模型输出 interaction：weave（交织倾向）、settle（形成沉积倾向），以及简短 explanation。两者均 0–1。模型结合两段话和感受描述判断，不简单求平均。

动画的确定性合成：

1. 前 32 秒两组各自运行，保留各自的轨迹。
2. 32–47 秒，每组前 round(48 × weave) 个粒子按固定顺序结对；连接轨迹逐渐显现。其余粒子保持自身轨迹，避免一切都被融合。
3. 47–57 秒，结对轨迹的 round(配对数 × settle) 对转为薄层网纹。未沉积的线条逐渐淡出。
4. 薄层颜色混合两个来源；基础透明度取两组残留透明度的均值。两组既有轨迹也保留一部分，由各自 trace 决定。
5. 每个转成薄层的粒子同步淡出，不新增“下一代”蜜蜂。生成的是线条与结构。

> 第 2、3 条的 round 一律为 half-up（`.5` 进位），视觉与声音两端必须同解。实现见 `web/visual/js/config.js` 的 `roundHalfUp`、`09-声音设计/build_score.py` 的 `round_half_up` 与 `web/audio/audio.mjs` 的同名函数；不要用 Python 内置 `round()`（银行家舍入）。理由与案例见 docs/DECISIONS.md（2026-09-11）。

weave 或 settle 为零时，允许没有明显融合或沉积；不强迫每种经历产生伤痕。低值案例也是作品的一部分。正向经历可以生成平稳、松散或持久的结构，不设“一句安慰清除全部残留”的规则。

## 人体与声音

外轮廓保持稳定；内部结构改变。不模拟疾病或器官受损。声音首轮仅随 energy 调整纹理密度，设置总音量上限；音色和现场音量交给 Scott 调整。无声音时叙事仍须成立。
