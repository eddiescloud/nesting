# TODO

更新：2026-09-12（最新为「AI 的理解过程」轮，读解带四层已接入；另有并发进行中的「蜂声音」模块；全量 **263** 项测试全过，计入跨模块协议 31 项则为 294。**计数会随任一模块增删测试而变，且本项目存在并发编辑，勿写死**——以 `docs/VERIFICATION_STATUS.md` 的复算命令跑出来的为准。本轮同步了三处过期计数、一条过期的 Blocked 结论，并把 HANDOFF 缺失的两轮记录补上）。

## Doing

- 等用户提供 `NESTING_API_KEY` 后跑一次 DeepSeek 真实调用，留下「模型确实参与」的证据（当前只有离线占位与未解码降级两条路径经过实测）。
- 语音识别（ASR）方案待用户确认：DeepSeek 无 audio 接口，转写目前是可替换槽位。
- **确认并发编辑方身份与分工边界**（2026-09-11 19:32 检测到：他人正在改 `web/visual/js/{config,timeline,bee}.js`、`web/interaction/{config,app}.js`，并新增 `web/audio/bee-voice.mjs` + 8 项测试与 `deliverables/演示诊断-20260911/` 截图）。在确认之前不动这三个目录下的代码，避免互相覆盖。

## Next

- [x] 节奏压缩（2026-09-12 完成，用户反馈「节奏可以更快点」）：DECOMPOSE 1.6/8、CAMERA 13/27、SWARM_VIEW 15/3/2.4、RIPPLE.firstAt 10.5、hive.js 19/8/4 与 25/9 与 20/13；`app.js` 演示 appearAt 15+3k。TIMELINE 边界不动（音频契约）。顺带修复存量 bug：宿主模式 `payload.bees` 未并入 BeeModule 参数，「别的蜂」从未在演示出现；已修，巢房字池现含全部演示语句。细节见 DECISIONS/HANDOFF。
- [x] AI 的理解过程（2026-09-12 完成）：新增 `web/visual/js/reading.js` + `config.READING`，2.5–11.6s 在画面上部依次展开语气 / 含义 / 分段 / 不确定四层；`voice.mjs::beeSpecFrom()` 附带 `decode`；新增 `test_reading.js` 41 项。全量 405 项通过。
- [ ] 后续候选（用户未确认）：越传越失真（远处的波更模糊）/ 蜂群传染（一只蜂触动其他蜂）/ 每字一圈小波。
- [ ] 理解过程与声音联动（扫描针配 tick 声）：需与声音模块协同，本轮只做视觉。

- [x] P0（方向校正第一步）：用一段转写样本做出一只可辨识、可追溯的蜜蜂；文字单元按顺序生成蜂体，已在浏览器完整 60 秒验证。真实录音/语气/含义映射仍待接入。
- [x] P0：重新设计输出协议，使单只蜜蜂的结构规格成为视觉输入（2026-09-11 完成：新增 `10-语言解码/decode.schema.json` 与 `beeSpec`；energy/dispersion/turbulence/trace 仍留在旧 output 里作脚手架，不再驱动单蜂）。

- [x] 方向复核（2026-09-11）：用户确认参赛 MVP 先移除人形，优先验收“文字结构生长一只蜂”；完整筑巢、人体关系与输入流程仍待后续确认。记录见 `DIRECTION_REVIEW_2026-09-11.md`。

- [ ] 核对 Scott 是否已有视觉工程、可用环境与分工，优先复用；若有，将本原型参数映射对接其环境。
- [x] P0（方向校正）：从参赛 MVP 主入口移除人形与旧粒子蜂群，先验收单只文字结构蜂；旧模块仅作回退脚手架。
- [x] P0：为单蜂 MVP 接入一段可重复的录音/转写样本，并记录文字单元与蜂体结构的逐项对应（2026-09-11 完成：`10-语言解码/映射表.md` 自动生成，含分段→部位→逐字单元→派生参数四张表）。
- [x] P0：确定录音采集、语气/含义解码的最小接口（2026-09-11 完成：`web/interaction/voice.mjs` 录音采集 + `POST /api/decode` 解码 + `beeSpec` 协议字段）。语音识别仍未接入，`--transcript` 由调用方提供。
- [x] P0：live 传输全路径实测（2026-09-11 完成，无需密钥）：`10-语言解码/test_decode.py::LiveTransportChecks` 4 项常驻测试，用本地模拟的 OpenAI 兼容上游跑真实子进程传输，覆盖合规返回、坏 JSON 重试、上游 500、密钥不外泄；网页端到端另测出「语气：压抑 · 方向：inward」画在蜂体下方。
- [x] P0：真实模型效果与延迟实测（2026-09-11 晚完成）：十句采样，平均 2.78 秒、成功率受 8 秒预算拖累约七成。据此把默认模型改为 `deepseek-chat`（`deepseek-flash` 为推理模型实测 21.4 秒）、预算放宽到 15 秒、新增多余字段剥离。详见 DECISIONS。
- [ ] P0：**画面改造（当前最大短板）**。实测蜂只占舞台不到 5%，舞台大片黑空；画布 1080×1920 竖屏被压进 821×900 横向容器（内容仅 506px 宽，左右各留约 157px 黑边）。待定向后改画布比例适配、蜂体尺度与可辨识度。
- [ ] P0：**云 ASR 接入**（用户已选路线，展位有稳定 WiFi）。需要第二个密钥（讯飞/阿里/腾讯一句话识别），接入时须同步 `/api/transcribe` 能力探测、界面隐私说明与 INTEGRATION_CONTRACT 的转写接入点三处。
- [ ] P1：色相新映射的美术确认。暖色基准 + 唯一冷色「疏离」是否符合 Scott 的最终视觉语言，需其确认。
- [x] P0：交互模块设置 visualUrl 指向 '/web/visual/index.html'，完成 iframe 联调。
- [ ] P1：使用现有三组人工参数验收视觉差异（声音接入与静音/退出测试已于 2026-09-11 随三方联调完成）。
- [x] 交互负责人：设置 config.js 的 audioModuleUrl '/web/audio/audio.mjs'，随 visualUrl 一并三方联调（2026-09-11 完成，无头 Chrome 实测 15 项断言全过）。
- [ ] 由 Scott/用户确认声音映射（09-声音设计/README.md）；映射与 0.8 总音量上限已经用户确认冻结，沉积公式为 round(round(48×weave)×settle)（2026-09-11 明确取整为 half-up，对齐 03 规则与视觉），仅剩音色与现场音量归 Scott。
- [ ] P0：离线展示版全屏、设备适配与连续 5 轮测试。
- [ ] P0：观众理解测试、说明、备份录屏及真实创作证据整理。
- [x] 清理 08-动画原型/ 残留目录（2026-09-11 完成）。
- [x] P1：删除 `08-视觉原型/{js,css,index.html,params}` 重复副本（2026-09-11 完成，用户确认）。`08-视觉原型/` 现仅含 README.md 与两个测试文件；两个测试均从 `../web/visual` 加载，删除后 128 项测试仍全过。备份在 `/tmp/nesting-cleanup-backup-20260911`。
- [x] P1：用户于 2026-09-12 明确授权，将本项目建成独立 Git 仓库并发布到 `eddiescloud/nesting` private 远端；提交只限项目目录，隐藏工具记忆、本地密钥和缓存不纳入。
- [ ] P2：相位表仍为 3 份副本（`build_score.py` / `session.mjs` / `web/visual/js/config.js`）加 3 套命名（calm·swarm-a… / opening·first… / idle·enter_a…）。2026-09-11 已加零风险守卫 `test_score.py::test_phase_table_consistent_across_modules`（任一副本漂移即失败）。真正收敛为单一来源需让浏览器端从 JSON 异步读取，会改动同步加载的共享 config，须单独设计。
- [x] P2：清理死代码（2026-09-11 完成，经复核修正判断）——删除 `audio.mjs` 未被引用的 `PHASES` 常量、`swarm.js` 第 115 行被遮蔽的 `groupEntered`。**核查后更正**：`swarm.getActiveCount()` 实际被 `08-视觉原型/test_headless.js` 调用，不是死代码，保留；`config.lerp()` 无调用方，但它是共享 config 的工具 API（与 `clamp`/`mixHSL`/`smoothstep` 同级），保留以免收窄他人可用的接口面。
- [x] P2：修正 `web/interaction/config.js` 注释里 `audioModuleUrl` 的示例路径与旧计数（2026-09-11 完成）。
- [ ] P2：`08-视觉原型/test_integration.js` 与 `web/interaction/tests/integration.test.mjs` 职责重叠（前者 31 项、后者 8 项，均验证同一 postMessage 协议）。去重或明确分工待定；`08-动画原型/test_integration.js` 已随残留目录删除。

## Later

- [ ] 语音、身体追踪、多人状态、更多材质与复杂声音。
- [ ] 协作规则：确定同事权限、分支／PR 流程；用户尚未提供邀请对象，不自动邀请。

## Blocked

- 真实 AI 接入与语义/延迟验收：~~用户要求暂缓~~——接入已于 2026-09-11 用户决定恢复（DeepSeek `deepseek-flash`，代码与传输层均已就绪并已测），当前**唯一阻塞是本机没有 `NESTING_API_KEY`**，提供后即可实测；不阻塞离线演示。
- 最终参赛验收：赛事原始规则、设备条件及提交要求未核验；若要求真实 AI，必须完成接入后再宣称达标。
- 声音最终音色/现场音量：等 Scott 现场调整；占位合成已升级一轮（失谐铺底/限带噪声颗粒/下滑脉冲/柔起泛音），不阻塞联调。

## Done

- [x] 2026-09-11（蜂巢改造轮，用户要求「组成人体改为组成蜂巢、更艺术」，方向选择「巢由语言筑成」）：
  - 新增 `web/visual/js/hive.js`（HiveModule）：程序化水滴形六边形巢脾（平顶六边形按列排布、sin 轮廓收放、从巢心向外排序，全部确定性）；23–38 秒蜂影（主蜂同源印记）从黄金角环形出发落进巢房，落定后巢房六边形短描边点亮（会徽风缺口边），房内填入一个来自现场语句的字——主蜂原句在前、其他蜂的句子随后循环取用；31–42 秒整面巢脾淡影渐显。节奏与旧人体贴图一致（progress 24→40s）。
  - 接线：`bee.js` 两处插值改接 hive（主蜂向巢心 `hive.heart`、别的蜂向巢房 `hive.target`）；`timeline.js` 绘制顺序 waveform → hive → bee；`main.js` 移除贴图异步加载改接 HiveModule；`index.html` 脚本标签替换。
  - 人体贴图降为脚手架：`body-texture.js` + `assets/human-front.svg` 保留但入口不再加载（与人形同等待遇）；`test_body_texture.js` 保持 12 项（绘制顺序契约移交 test_hive.js）。
  - 新增 `08-视觉原型/test_hive.js` 23 项（梨形布局、确定性、巢位、房内字可追溯、暂停/回退/重播、绘制顺序、无蜂绘制不抛错）。
  - 无头 Chrome 实测：演示句「急促」26/35/44 秒截图（蜂影飞入 → 巢心点亮 → 巢脾成形），零异常；截图存 `deliverables/蜂巢改造-20260911/`。
  - 全量测试通过（蜂 56 + 蜂巢 23 + 人体贴图 12 + 声纹 20 + 无头 50 + 交互/服务器/声音各套件均绿；计数以 VERIFICATION_STATUS 复算为准）。
- [x] 概念、分镜、参数、提示词与人工示例初稿。
- [x] 离线校验和预留接口；2026-09-08 复核 12 项测试与 demo 通过。
- [x] 2026-09-08：后续开发计划、六份恢复文件及交接；修正旧计划与暂缓接入的冲突。
- [x] 2026-09-08：人形 / 蜂群运动 / 交织与沉积动画模块完成。web/visual/ 下 HTML5 Canvas 实现，60 秒六阶段闭环，支持独立运行与 iframe 宿主协议（INTEGRATION_CONTRACT v0.1）。42 项无头测试通过。模块设计文档见 08-视觉原型/README.md。

- [x] 交互层：开始、三个故事、输入检查、暂停/继续、重播、退出、重置与可选全屏。
- [x] 独立视觉/音频桥接及对接协议；39 项自动测试通过，桌面/手机浏览器操作验证完成。真实成品模块联调仍待完成。
- [x] 2026-09-08（声音 AI）：声音设计与音画同步——09-声音设计/：score.schema.json、build_score.py（复用 07 校验器，确定性推导）、render_score.py（参考 WAV）、test_score.py（14 项通过）；示例 score 与 60 秒参考 WAV 落盘。web/audio/audio.mjs 按 INTEGRATION_CONTRACT 实现 createAudio()（宿主时钟驱动、默认静音、防暂停爆音）+ 9 项无头测试，层参数与离线 score 交叉校验一致。新增 docs/INTERFACES.md（统一运行环境与模块接口）。07 的 12 项测试与 demo 回归通过；未修改 04/07 及其他 AI 模块。
- [x] 2026-09-08（声音 AI，用户确认后）：编号重排——08-动画原型→08-视觉原型、08-声音设计→09-声音设计，全部引用同步替换（0 残留）；声音映射与 0.8 总音量上限经用户确认冻结；占位音色补充升级（失谐铺底/限带噪声颗粒/下滑脉冲/柔起泛音，运行时主链低通），离线 14 项 + 运行时 9 项测试保持通过，新参考 WAV 峰值 0.482。详见 DECISIONS 两条新决策。
- [x] 2026-09-09（全模块联调修复轮）：修复跨模块集成测试全部失败。视觉模块 4 个 bug（hostAdapter timeline 引用、沉积粒子淡出冲突、externalClock 模式、source/sessionId 安全校验）；声音模块 3 个 bug（沉积公式多除 2 对齐 03 规则、frame 支持 playing 暂停、spawn 立即调度）；交互测试 3 处路径错误。重新生成 score.json，更新测试期望值。跨模块集成 8/8 全过。（当日记录的「122 项」口径有误：重复计入了已含在交互 39 项内的交互服务器 10 项，实为 112 项。）
- [x] 2026-09-11（契约修复轮）：修复两项可复现契约缺陷。P1-1 `score.schema.json` 的 `events.maxItems` 40→54，使 `weave=1.0/settle=1.0` 这一合法参数不再让 `build_score.py` 报「数组长度错误」（旧上限下 `settle=1.0` 时 `weave ≥ 0.71875` 即失败）。P1-2 新增 half-up 取整实现（`build_score.round_half_up`、`config.roundHalfUp`、`audio.mjs` 本地 `roundHalfUp`），并将同根因的 `build_score.r4`（四位小数）也改为 half-up，与 `audio.mjs` 的 `r4` 同解，消除 Python 银行家舍入与 JS `Math.round` 在 48×weave 恰为 .5、以及小数第五位为 5 时的数值漂移。新增回归测试 14 项（声音离线 +4、视觉无头 +8、audio +2），同步 `08-视觉原型/js/` 副本，新增 `.gitignore`。全量 126 项测试通过，跨模块集成 8/8。详见 DECISIONS 与 HANDOFF。
- [x] 2026-09-11（交互收尾轮）：`web/interaction/config.js` 的 `audioModuleUrl` 由 `null` 改为 `'/web/audio/audio.mjs'`，三方联调（交互+视觉+声音同时运行）跑通。无头 Chrome + CDP 实测 15 项断言全过（声音按钮出现、AudioContext `running`、播放中调度声源且主增益 > 0、暂停立即静音且画面冻结、继续接续、退出静音复位回 00:00，无 404/异常/console.error）。全量 128 项测试复跑通过。顺带修正 HANDOFF/PROJECT_CONTEXT 的过期计数（18→19、11→12），并确认「视觉就绪后舞台恒显示作品预览、分镜仅作回退」为设计而非缺陷（已写入 HANDOFF Current State）。
- [x] 2026-09-11（ASR 接缝轮）：把「转写走可替换接口」做成显式可测的接口，而不是隐含做法。
  - 新增 `GET /api/transcribe` 能力探测（如实返回 `available:false`）与 `POST /api/transcribe`（501，不接收、不保存、不转发音频）；新增 2 项服务端测试。
  - `voice.mjs` 新增 `transcribeCapability()`：探测失败一律按不可用处理，绝不猜测可用；新增 2 项测试。界面说明文字改由服务端如实给出，不再写死。
  - 新增 `docs/VERIFICATION_STATUS.md`：验证状态总表（已实测 / 未验证分列 + 复现命令）。
  - `04-AI转译/系统提示词.md` 增加指向 `10-语言解码/` 的说明：当前生效的解码契约在那个目录，本目录只留旧脚手架。
  - `docs/INTEGRATION_CONTRACT.md` 新增「转写（ASR）接入点」小节，写明接入时必须同步的三处与隐私约束。
  - 全量 194 项测试通过（语言解码 24→28、voice 6→8、服务器 15→17）。
- [x] 2026-09-11（可追溯轮）：把「可追溯」从声明变成看得见、防得住的东西。
  - 映射表生成器从 `/tmp` 脚本搬进模块：`derive_bee.py --md`，README 的重新生成命令变成真的可执行命令（此前"生成脚本见交接记录"是空头承诺）。
  - 新增防漂移守卫 `test_mapping_table_is_not_stale`：改了派生公式却没重新生成映射表就直接失败。**已实测有效**——把翅膀频率公式 1.6 改成 1.7，测试立刻报错并给出重新生成命令；恢复后 32 项全过。
  - 界面新增可追溯证据面板：解码后在「正在经历」里显示逐项对应规则（默认折叠、退出清空），观众与 Scott 可以逐行核对"这只蜂的参数是怎么来的"。
  - 全量 200 项测试通过（语言解码 30→32、voice 8→10）。
- [x] 2026-09-11（演示轮，用户要求「先做 demo」）：做出可放心演示的离线版本。
  - 新增 3 条人工占位演示素材（`10-语言解码/examples/decode.demo-*-手工占位.json`）：压抑 / 平稳 / 急促，语气不同 → 蜂的主色相 30.6 / 35.8 / 54.6、姿态 curl / level / open、翅膀频率 2.64 / 2.00 / 3.08，肉眼可辨。
  - 新增 `GET /api/demo`：返回演示素材 + 现算的蜂体规格与证据；不依赖模型密钥。2 项服务端测试（含「三条素材必须产出不同的蜂」）。
  - 交互页新增演示句按钮与 `?demo=1` 直达入口；画面与状态栏都标注「人工占位示例（未调用模型）」；证据面板同步显示。
  - 新增一键启动器 `演示.command`（起服务 + 打开 `?demo=1`）与 `docs/DEMO.md`（演示步骤、红线、故障备份）。
  - 无头 Chrome 实测：`?demo=1` 直达「说一句话」；点「急促」hue=49、点「压抑」hue=33，画布标注「人工占位示例（未调用模型）· 语气：…」，证据面板 10–11 行，零异常、零 404。
  - 功能层（真实模型调用、ASR）按用户要求推迟。
- [x] 2026-09-11（视觉改造轮，用户反馈）：按「蜜蜂太丑 / 节奏太慢 / 停顿无聊」三条意见改造视觉。
  - **造型**：腹部改为交错明暗的腹节带，一个字符一节（真蜂有条纹，同时让「由文字组成」看得见）；新增胸节绒毛感、复眼与高光、两对翅（含翅脉）、三对足、尾针、柔光。`spec.spacing` 改为拉伸腹部长度。
  - **节奏**：整句停留 5→2.5 秒，拆字飞入 5–20 秒→2.5–10 秒，蜂体约 15 秒成形（原来 20 秒才开始成形）。阶段文字同步改为「整句与拆解 / 蜂体成形 / 存留 / 声音进入 / 声音远去 / 留存」。
  - **声纹**：新增 `web/visual/js/waveform.js`（模块契约同上），波形由 `09-声音设计/build_score.py` 推导的 score 驱动，**不重复实现声音公式**；静音时照常显示；只有铺底声时不画颗粒、纹理层活跃时才出颗粒，沉积事件到点画扩散圆环。服务端新增 `GET /api/score?id=` 复用推导器，交互层随 load 下发 `payload.score`。独立原型加载 `web/visual/params/score.demo.json`。
  - 新增 `08-视觉原型/test_waveform.js` 14 项，含**跨模块守卫**：波形包络与 `web/audio/audio.mjs` 的 `layerEnvelope` 逐值一致（最大偏差 0）。
  - 顺带修掉一个坏命令：`build_score.py --output web/visual/params/demo.json` 在方向变更后必然报「对象存在缺失或多余字段」（demo.json 多了 sourceText），文档已改为用 `04-AI转译` 的输出。
  - 无头 Chrome 实测：蜂体暖色像素 2814 → 24443 → 30712（2→11→19 秒），声纹冷色像素 107 → 638，网页演示 14 秒时蜂体已成形态。全量 223 项测试通过。
- [x] 2026-09-11（造型简化轮，用户反馈「波形放背景 / 偏旁组轮廓 / 内填原句」）：
  - **声纹铺满背景**：不再是一条带，改为 24 条横向流动曲线铺满整幅画面，静态底噪 alpha 0.14、随响度增到 0.48；颗粒与沉积圆环同步放大。实测顶部区域亮点 2122 → 5952。
  - **修掉一个假验证**：`timeline.draw()` 里「有 bee 就 return」导致声纹从未被绘制；我上一轮测到的「冷色像素」其实是蜂翅膀。已修顺序并在 `test_waveform.js` 加守卫测试（实际顺序 waveform,bee）。
  - **蜂改为偏旁轮廓 + 原句填充**：轮廓由偏旁部首沿蜂形（头/胸/腹/两对翅/触角）排布组成，偏旁按原句字符顺序取用并循环；内部裁剪填入刚说的那句话。内置「字 → 偏旁」表覆盖演示句与常用字（你→亻、添→氵、慢→忄…），表外字按字符码确定性回退，文档写明那不是字形拆解。
  - 蜂测试 23 → 37 项（新增偏旁来源、轮廓取样、填充落点、画布实绘断言），声纹 14 → 17 项。全量 240 项通过。
- [x] 2026-09-11（群蜂与蜂声轮，用户描述「视野放大 → 蜂变小变多 → 语气决定颜色与声音」）：
  - **视野拉远**：新增 `config.CAMERA`（17 秒前近景、17–34 秒拉到 0.44）与 `timeline.cameraScale()`；缩放只作用于蜂这一层，声纹背景始终铺满画面。实测蜂的成像高度 307 → 147 像素。
  - **群蜂**：`config.SWARM_VIEW` + `bees` 载荷字段；每只蜂有自己的原句、偏旁轮廓、语气色相与出现时间，按黄金角确定性布点。实测两只别的蜂依次出现，三只蜂同框。
  - **每只蜂的声音**：新增 `web/audio/bee-voice.mjs`（8 项测试）——语气和缓 → 协和音程（根音/五度/八度）+ 慢起音；语气尖锐 → 掺小二度与三全音 + 硬起音 + 确定性噪声。音高由该只蜂的色相决定。**语气映射是艺术选择，不评价说话人**，未解码时取中性。
  - 修掉一个真 bug：`bee-voice.mjs` 在注入 AudioContext 时不建主增益，导致 `play()` 静默无声（测试先失败后定位）。
  - 实测：两只别的蜂出现时共新建 12 个振荡器；零异常零 404。蜂测试 37 → 49 项，声纹 17 → 20 项，全量 263 项通过。
- [x] 2026-09-11（轮廓换装轮，用户给的参考图）：蜂的轮廓由「偏旁部首字形」改为「会徽风短笔画」。
  - 每个字符 → 一个笔画代号（h 横 / s 竖 / p 撇 / d 点 / t 提 / j 竖钩 / g 横折 / c 弧），按原句字符顺序沿蜂形轮廓排布；笔画由路径 API 画短粗线段/弧/点/钩/折，方向沿轮廓切线，整体疏朗如会徽/图章。
  - 内部仍按原句折行填字，「由文字组成」的可追溯证据保留。
  - `bee.js` 移除 `RADICALS`/`CHAR_RADICAL`/`_radicalFor`/`this.radicals`/`state.radicals`，替换为 `STROKES`/`CHAR_STROKE`/`_strokeFor`/`this.strokes`/`state.strokes` 与新方法 `_drawStroke`。
  - `test_bee.js` 的 `recordingCtx` 扩展为 `texts[]`+`marks[]`，新增 7 项笔画断言（笔画代号非空、5 种 kind 全覆盖、多句不抛错）；49 → 56 项。
  - `08-视觉原型/README.md` 新增「轮廓规则」一节；`DECISIONS.md` 同步一条。
  - 仅触动 `web/visual/js/bee.js` 与 `08-视觉原型/test_bee.js`，与并发方正在做的「人体贴图衔接 / 蜂声音」无字段冲突；对方独立模块（body-texture.js / bee-voice.mjs）测试保持通过（12 + 8 项）。蜂的具体造型与「会徽/图章」参考图的契合度仍以观众最终观感为准。
- [x] 2026-09-11（语言传播涟漪轮，用户选「涟漪扩散」+「先做视觉可见的」）：新增**语言传播涟漪**——一句话说出口，从蜂体向外推出同心波纹，扩散到画面边缘消失。用户原话「不是想给别人一种理念，而是希望大家能够从这个角度意识到语言的重要性」，所以本轮做的是「让语言自己被看见」，不加任何文案说明。
  - 新增 `web/visual/js/ripple.js`（`RippleModule`，独立模块，接口与 bee/hive/waveform 一致）+ `config.RIPPLE` 参数。
  - 派生关系可追溯到语义：波速 = `baseSpeed + speedRange × (wingbeatHz − 1.6) / 1.8`（wingbeatHz 由语气强度派生 → 说得越激动传得越快）；波强度 = `0.45 + 0.55 × tension`（tension 含量与强度 → 分量越重波越粗）；波色沿用该句 hue。
  - 波面由沿切线的短笔画（线段/点/弧）组成，呼应会徽风笔画轮廓；越远越淡越细、笔画越稀疏，像是语言散开了。
  - **无状态**：半径只由「当前时间 − 发出时刻」算出 → 暂停冻结、倒退收回、重播清空，始终跟着宿主时间。
  - 图层：`waveform → hive → ripple → bee`，涟漪在蜂之下，波心被蜂挡住，只见向外推的环。
  - 新增 `08-视觉原型/test_ripple.js` 23 项（触发/扩散/消失/波速/强度/色相/倒退/重播/确定性/上限/画布实绘/零空转/60 秒 1801 帧）。
  - **刻意不动 `bee.js`**：并发方 19:32 与 23:32 两次改动该文件，独立模块可完全避开覆盖风险。
  - 全量 362 项通过（Node 275 + Python 87），无回归。
- [ ] 涟漪的后续增强（用户未确认，仅记录候选）：① 越传越失真——远处波更模糊破碎（话传远了会变样）；② 蜂群传染——一只蜂成形时触动附近其他蜂（语言影响别的语言）；③ 每字一圈小波——拆解阶段逐字飞入时各推一圈细波。三项都建立在现有 `ripple.js` 之上，互不冲突。
